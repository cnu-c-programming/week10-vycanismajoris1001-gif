#include <stdio.h>

int main() {
  int *space = malloc(10*sizeof(int));
  struct Point *next = &a;
  struct Point a = {a.name, a.score, &b};
  struct Point b = {b.name, b.score, &c};
  struct Point c = {c.name, c.score, NULL};

  for (struct Point *p =

  scanf("%s %s %s",  ,&name, &score);
add, delete, print
  free(space);
}

struct Point {
  string name;
  int score;
  struct Point *next;

  
};
